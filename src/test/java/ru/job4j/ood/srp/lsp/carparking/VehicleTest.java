/*package ru.job4j.ood.srp.lsp.carparking;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import ru.job4j.ood.srp.lsp.storagefood.ExpiryPercent;
import ru.job4j.ood.srp.lsp.storagefood.Food;

import static org.assertj.core.api.Assertions.*;
import static org.junit.jupiter.api.Assertions.assertEquals;

@Disabled("Временно отключено, так как есть ошибки")
class VehicleTest {

    @Test
    void createVehicle() {
        Vehicle result = new Vehicle(1, Type.CAR);
        Vehicle expected = new Vehicle(1, Type.CAR);
        assertEquals(result, expected);
    }

}*/